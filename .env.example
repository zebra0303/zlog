# ============================================================
# 🦓 ZLOG 환경변수 설정
# ============================================================
# 이 파일을 복사해서 .env 파일로 만들어 사용하세요:
#   cp .env.example .env
#
# .env 파일은 Git에 커밋되지 않으므로 안전하게 비밀 정보를 저장할 수 있습니다.
# ============================================================

# ---- 서버 설정 ----

# 서버(Hono)가 실행될 포트 번호
# Vite 개발서버가 이 포트로 API 요청을 프록시합니다.
PORT=3000

# 블로그의 외부 접속 URL (CORS에 사용됨)
# - 프로덕션(Docker): 실제 도메인 (예: https://myblog.example.com)
# - 개발 모드(npm run dev): http://localhost:5173 (Vite 개발서버 주소)
SITE_URL=http://localhost:3000

# ---- 관리자 계정 ----
# 처음 서버를 실행하면 이 정보로 관리자 계정이 자동 생성됩니다.

# 관리자 로그인 이메일
ADMIN_EMAIL=admin@example.com

# 관리자 로그인 비밀번호 (반드시 변경하세요!)
ADMIN_PASSWORD=changeme

# 블로그에 표시될 관리자 이름
ADMIN_DISPLAY_NAME=Blog Owner

# 관리자 핸들 (URL에 사용됨, 영문 소문자 권장)
ADMIN_BLOG_HANDLE=admin

# ---- 보안 ----

# JWT 토큰 암호화 키 (반드시 변경하세요!)
# 아래 명령어로 안전한 랜덤 값을 생성할 수 있습니다:
#   openssl rand -base64 32
JWT_SECRET=please-change-this

# ---- Federation (블로그 간 구독) ----

# 구독한 블로그의 새 글을 확인하는 주기 (분 단위)
WEBHOOK_SYNC_INTERVAL=15

# ---- OAuth 로그인 (선택사항) ----
# 방문자가 GitHub/Google 계정으로 댓글을 남길 수 있습니다.
# 사용하지 않으면 주석 처리된 채로 두세요.

# GitHub OAuth
# 설정 방법: https://github.com/settings/developers → "New OAuth App"
# Authorization callback URL: {SITE_URL}/api/oauth/github/callback
# GITHUB_CLIENT_ID=please-change-this
# GITHUB_CLIENT_SECRET=please-change-this

# Google OAuth
# 설정 방법: https://console.cloud.google.com/apis/credentials → "OAuth 2.0 Client"
# 승인된 리디렉션 URI: {SITE_URL}/api/oauth/google/callback
# GOOGLE_CLIENT_ID=please-change-this
# GOOGLE_CLIENT_SECRET=please-change-this